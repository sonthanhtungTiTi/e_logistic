### 4.4.3 Test case chức năng Xác nhận lấy hàng (UC-12 Confirm Pickup & Batch Pickup)

#### Bảng 4.6 Test case chức năng Xác nhận lấy hàng

| Test ID | Mô tả | Điều kiện trước | Các bước test | Dữ liệu Test | Kết quả mong muốn | Kết quả thực tế | Pass/ Fail |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :---: |
| **TC_UCXNDH_01** | Kiểm thử chức năng xác nhận lấy hàng khi nhập/quét đầy đủ thông tin hợp lệ. | Shipper đã đăng nhập thành công và đang ở tuyến thu gom. Đơn hàng ở trạng thái `READY_TO_PICK`. | 1. Chọn đơn hàng cần lấy.<br>2. Quét mã QR trên kiện hàng.<br>3. Kiểm tra trọng lượng/kích thước.<br>4. Yêu cầu Seller ký tên.<br>5. Nhấn nút "Xác nhận lấy hàng". | Mã vận đơn: "ELG-VN-67044822".<br>Quét QR: "ELG-VN-67044822".<br>Trọng lượng: "1.0 kg".<br>Chữ ký: "sig_valid.png".<br>GPS: "21.0020, 105.8130". | Hiển thị thông báo "Xác nhận lấy hàng thành công!". Đơn hàng chuyển sang trạng thái "PICKED_UP", sinh bản ghi ePOH và Audit Log. | Hiển thị thông báo "Xác nhận lấy hàng thành công!". Đơn hàng chuyển sang trạng thái "PICKED_UP", sinh bản ghi ePOH và Audit Log. | Pass |
| **TC_UCXNDH_02** | Kiểm thử chức năng xác nhận lấy hàng khi nhập mã vận đơn thủ công (QR mờ/rách). | Shipper đã đăng nhập thành công. Đơn hàng ở trạng thái `READY_TO_PICK`. Camera không quét được QR. | 1. Chọn ô nhập mã thủ công.<br>2. Nhập chính xác mã vận đơn.<br>3. Thu thập chữ ký Seller.<br>4. Nhấn nút "Xác nhận lấy hàng". | Mã vận đơn nhập tay: "ELG-VN-67044823".<br>Chữ ký: "sig_manual.png".<br>GPS: "21.0050, 105.8150". | Hiển thị thông báo "Xác nhận lấy hàng thành công!". Đơn hàng chuyển sang trạng thái "PICKED_UP". | Hiển thị thông báo "Xác nhận lấy hàng thành công!". Đơn hàng chuyển sang trạng thái "PICKED_UP". | Pass |
| **TC_UCXNDH_03** | Kiểm thử xác nhận lấy hàng khi phát hiện chênh lệch khối lượng thực tế và tự động phụ thu cước. | Shipper đã đăng nhập thành công. Đơn hàng ở trạng thái `READY_TO_PICK`, khối lượng khai báo 1.0 kg. | 1. Quét mã vận đơn.<br>2. Cân lại kiện hàng, nhập trọng lượng 3.0 kg.<br>3. Chụp ảnh kiện hàng đối chứng.<br>4. Lấy chữ ký Seller và nhấn "Xác nhận lấy hàng". | Mã vận đơn: "ELG-VN-67044824".<br>Trọng lượng gốc: "1.0 kg".<br>Trọng lượng thực tế: "3.0 kg".<br>Ảnh kiện hàng: "parcel_proof_03.jpg".<br>Chữ ký: "sig_weight.png". | Hiển thị thông báo "Xác nhận lấy hàng thành công!". Cập nhật actualWeight = 3.0 kg, tự động phụ thu cước 10,000đ, bật cờ weightDiscrepancy = true, đơn chuyển PICKED_UP. | Hiển thị thông báo "Xác nhận lấy hàng thành công!". Cập nhật actualWeight = 3.0 kg, tự động phụ thu cước 10,000đ, bật cờ weightDiscrepancy = true, đơn chuyển PICKED_UP. | Pass |
| **TC_UCXNDH_04** | Kiểm thử xác nhận lấy hàng khi quét nhầm mã QR của đơn hàng khác. | Shipper đang xử lý đơn hàng "ELG-VN-0004" ở trạng thái `READY_TO_PICK`. | 1. Quét mã QR trên kiện hàng khác.<br>2. Nhấn nút "Xác nhận lấy hàng". | Mã đơn mục tiêu: "ELG-VN-0004".<br>Mã QR quét được: "ELG-VN-9999". | Hiển thị thông báo lỗi "Mã vận đơn không khớp.". Đơn hàng giữ nguyên trạng thái READY_TO_PICK. | Hiển thị thông báo lỗi "Mã vận đơn không khớp.". Đơn hàng giữ nguyên trạng thái READY_TO_PICK. | Pass |
| **TC_UCXNDH_05** | Kiểm thử xác nhận lấy hàng khi thiết bị tắt hoặc mất định vị GPS. | Shipper đã đăng nhập. Tắt dịch vụ vị trí GPS trên thiết bị mobile. | 1. Quét mã vận đơn hợp lệ.<br>2. Lấy chữ ký Seller.<br>3. Nhấn nút "Xác nhận lấy hàng". | GPS Lat/Lng: null.<br>Chữ ký: "sig_nogps.png". | Hiển thị thông báo "Xác nhận lấy hàng thành công!". Đơn chuyển PICKED_UP và tự động đánh dấu cờ gpsMissing = true để QC đối soát. | Hiển thị thông báo "Xác nhận lấy hàng thành công!". Đơn chuyển PICKED_UP và tự động đánh dấu cờ gpsMissing = true để QC đối soát. | Pass |
| **TC_UCXNDH_06** | Kiểm thử xác nhận lấy hàng khi bỏ trống chữ ký Seller. | Shipper đã đăng nhập thành công. Đơn hàng ở trạng thái `READY_TO_PICK`. | 1. Quét mã vận đơn.<br>2. Bỏ trống khung chữ ký Seller.<br>3. Nhấn nút "Xác nhận lấy hàng". | Chữ ký Seller: "" (bỏ trống). | Hiển thị thông báo "Yêu cầu chữ ký Seller xác thực bàn giao.". Đơn hàng giữ nguyên trạng thái READY_TO_PICK. | Hiển thị thông báo "Yêu cầu chữ ký Seller xác thực bàn giao.". Đơn hàng giữ nguyên trạng thái READY_TO_PICK. | Pass |
| **TC_UCXNDH_07** | Kiểm thử tính năng Idempotency khi mất mạng và đồng bộ lặp lại từ Offline Queue. | Shipper thực hiện lấy hàng ở chế độ Offline. Đơn hàng đã lưu vào Queue thiết bị. | 1. Bật mạng trở lại.<br>2. Hệ thống tự động gửi Request 1 với clientOfflineId = "UUID_777".<br>3. Hệ thống tiếp tục gửi trùng Request 2 với clientOfflineId = "UUID_777". | Request 1 & 2 trùng clientOfflineId = "UUID_777". | Request 1 ghi nhận lấy hàng thành công. Request 2 trả thông báo "Xác nhận lấy hàng đã được ghi nhận trước đó (Offline Sync)" mà không ghi trùng CSDL. | Request 1 ghi nhận lấy hàng thành công. Request 2 trả thông báo "Xác nhận lấy hàng đã được ghi nhận trước đó (Offline Sync)" mà không ghi trùng CSDL. | Pass |
| **TC_UCXNDH_08** | Kiểm thử ghi nhận lấy hàng thất bại khi Seller từ chối ký biên bản. | Shipper đã đến địa chỉ Seller, Seller không đồng ý giao hàng và từ chối ký. | 1. Chọn "Báo lấy hàng thất bại".<br>2. Chọn lý do "Seller từ chối ký biên bản bàn giao".<br>3. Nhập ghi chú nguyên nhân.<br>4. Nhấn "Gửi báo cáo thất bại". | Lý do: "SELLER_REFUSED_SIGNATURE".<br>Ghi chú: "Seller đổi ý không giao hàng". | Hiển thị thông báo "Ghi nhận lấy hàng thất bại thành công". Trạng thái đơn hàng chuyển sang PENDING_PICKUP_RESCHEDULE, ghi nhận Audit Log. | Hiển thị thông báo "Ghi nhận lấy hàng thất bại thành công". Trạng thái đơn hàng chuyển sang PENDING_PICKUP_RESCHEDULE, ghi nhận Audit Log. | Pass |
| **TC_UCXNDH_09** | Kiểm thử bảo mật khi gọi API xác nhận lấy hàng mà chưa đăng nhập. | Chưa đăng nhập (Không truyền Header Authorization hoặc Token hết hạn). | 1. Gửi request trực tiếp đến API confirm-pickup. | Header Authorization: null. | Hiển thị thông báo lỗi "Unauthorized" / "Token không hợp lệ". HTTP Status 401. | Hiển thị thông báo lỗi "Unauthorized" / "Token không hợp lệ". HTTP Status 401. | Pass |
| **TC_UCXNDH_10** | Kiểm thử phân quyền RBAC khi tài khoản Khách mua (BUYER) cố tình gọi API lấy hàng. | Đăng nhập bằng tài khoản có role `BUYER`. | 1. Gửi request xác nhận lấy hàng với Token của role BUYER. | User Role: "BUYER". | Hiển thị thông báo lỗi "Forbidden: Bạn không có quyền thực hiện thao tác này". HTTP Status 403. | Hiển thị thông báo lỗi "Forbidden: Bạn không có quyền thực hiện thao tác này". HTTP Status 403. | Pass |
| **TC_UCXNDH_11** | Kiểm thử State Machine Guard khi Seller hủy đơn hàng trước thời điểm Shipper lấy hàng. | Đơn hàng bị Seller hủy (`CANCELLED`) trong lúc Shipper đang di chuyển tới. | 1. Quét mã vận đơn của đơn hàng đã bị hủy.<br>2. Nhấn nút "Xác nhận lấy hàng". | Trạng thái đơn trong DB: "CANCELLED". | Hiển thị thông báo "Đơn hàng đã bị hủy bởi Seller, không thể lấy hàng.". HTTP Status 409 Conflict. | Hiển thị thông báo "Đơn hàng đã bị hủy bởi Seller, không thể lấy hàng.". HTTP Status 409 Conflict. | Pass |
| **TC_UCXNDH_12** | Kiểm thử Driver Ownership Guard khi Shipper xử lý nhầm đơn của tuyến khác. | Đơn hàng được phân công cho tài xế `DRV_01`. Tài xế `DRV_02` thực hiện quét lấy hàng. | 1. Tài xế DRV_02 quét mã vận đơn gán cho DRV_01.<br>2. Nhấn nút "Xác nhận lấy hàng". | Đơn gán cho: "DRV_01".<br>Token gọi API: "DRV_02". | Hiển thị thông báo "Đơn hàng không nằm trong danh sách tuyến thu gom được gán cho bạn.". HTTP Status 403 Forbidden. | Hiển thị thông báo "Đơn hàng không nằm trong danh sách tuyến thu gom được gán cho bạn.". HTTP Status 403 Forbidden. | Pass |
| **TC_UCXNDH_13** | Kiểm thử tính cước theo trọng lượng quy đổi thể tích (Volumetric Weight). | Đơn hàng có trọng lượng thực tế 1.0 kg, kích thước cồng kềnh 50 x 40 x 30 cm. | 1. Quét mã vận đơn.<br>2. Nhập kích thước 50 x 40 x 30 cm.<br>3. Nhấn nút "Xác nhận lấy hàng". | Trọng lượng thực: 1.0 kg.<br>Kích thước: 50 x 40 x 30 cm. | Tính trọng lượng thể tích = (50x40x30)/5000 = 12.0 kg. Cập nhật chargeableWeight = 12.0 kg, tự động phụ thu cước cồng kềnh, chuyển PICKED_UP. | Tính trọng lượng thể tích = 12.0 kg. Cập nhật chargeableWeight = 12.0 kg, tự động phụ thu cước cồng kềnh, chuyển PICKED_UP. | Pass |
| **TC_UCXNDH_14** | Kiểm thử bắt buộc ảnh chụp đối chứng khi phát hiện chênh lệch khối lượng/kích thước. | Shipper nhập trọng lượng thực tế 4.0 kg (lệch so với 1.0 kg gốc), nhưng không chụp ảnh. | 1. Nhập trọng lượng thực tế 4.0 kg.<br>2. Bỏ trống ảnh chụp kiện hàng.<br>3. Nhấn nút "Xác nhận lấy hàng". | actualWeight: 4.0 kg.<br>parcelImageUrl: null. | Hiển thị thông báo "Bắt buộc chụp ảnh kiện hàng đối chứng khi phát hiện lệch cân nặng/kích thước.". HTTP Status 422 Unprocessable Entity. | Hiển thị thông báo "Bắt buộc chụp ảnh kiện hàng đối chứng khi phát hiện lệch cân nặng/kích thước.". HTTP Status 422 Unprocessable Entity. | Pass |
| **TC_UCXNDH_15** | Kiểm thử tính sẵn sàng (Resilience) khi dịch vụ gửi thông báo FCM/SMS bị sự cố. | Dịch vụ FCM/Notification gặp lỗi 500 hoặc Timeout. | 1. Shipper xác nhận lấy hàng thành công.<br>2. Hệ thống gọi dịch vụ gửi thông báo. | Notification Service: Mock 500 Internal Error. | Đơn hàng vẫn Commit PICKED_UP thành công, lưu ePOH thành công. Đẩy sự kiện Notification vào Outbox Queue để retry mà không làm sập giao dịch. | Đơn hàng Commit PICKED_UP thành công. Event Notification đẩy vào Outbox Queue retry thành công. | Pass |
| **TC_UCXNDH_16** | Kiểm thử Payload Guard khi tải lên ảnh chữ ký / ảnh chụp quá dung lượng (>5MB). | Shipper chọn tải lên file ảnh có dung lượng 10MB hoặc file mã độc (`.exe`). | 1. Tải lên file ảnh chữ ký > 5MB.<br>2. Nhấn nút "Xác nhận lấy hàng". | Dung lượng file: 10MB.<br>Định dạng: signature.exe. | Hiển thị thông báo lỗi dung lượng hoặc định dạng file không hợp lệ. HTTP Status 413 Payload Too Large / 400 Bad Request. | Hiển thị thông báo lỗi dung lượng hoặc định dạng file không hợp lệ. HTTP Status 413 Payload Too Large / 400 Bad Request. | Pass |
| **TC_UC12_17** | Gom lô thành công 100% (Batch Full Flow). | Shipper đứng tại Shop có 5 đơn hàng ở trạng thái `READY_TO_PICK`. | 1. Quét 5 mã đơn.<br>2. Nhấn Chốt ePOH.<br>3. Seller ký tên 1 lần.<br>4. Nhấn Xác nhận. | 5 đơn hàng cùng Shop.<br>1 chữ ký signatureImageUrl.<br>GPS hợp lệ. | Cả 5 đơn chuyển PICKED_UP, sinh đúng 01 bản ghi PickupConfirmation (ePOH) chứa 5 mã đơn, HTTP Status 200 OK. | Cả 5 đơn chuyển PICKED_UP, sinh đúng 01 bản ghi ePOH chứa 5 mã đơn, HTTP Status 200 OK. | Pass |
| **TC_UC12_18** | Gom một phần (Partial Batch Pickup). | Tuyến có 10 đơn, Shipper quét 8 đơn, 2 đơn Shop chưa chuẩn bị xong. | 1. Quét 8 đơn.<br>2. Chọn lý do 2 đơn chưa đóng gói.<br>3. Seller ký chốt 8 đơn. | 8 đơn hợp lệ, 2 đơn gán cờ SELLER_NOT_READY. | 8 đơn quét được chuyển PICKED_UP (gắn ePOH), 2 đơn còn lại chuyển PENDING_PICKUP_RESCHEDULE đẩy về ca sau. | 8 đơn chuyển PICKED_UP, 2 đơn chuyển PENDING_PICKUP_RESCHEDULE. | Pass |
| **TC_UC12_19** | Quét lẫn kiện hàng của Shop khác vào phiên. | Phiên gom của SHOP_A, Shipper quét mã thuộc sở hữu của SHOP_B. | 1. Mở phiên gom Shop A.<br>2. Quét mã kiện hàng Shop B. | Tracking code của SHOP_B. | App rung báo lỗi, hiển thị popup "Kiện hàng không thuộc điểm lấy của Shop này". Không thêm vào danh sách lô. | App rung báo lỗi, từ chối thêm mã Shop B vào lô gom. | Pass |
| **TC_UC12_20** | Quét trùng 1 mã kiện hàng trong cùng phiên (Duplicate Scan Protection). | Mã đơn "ELG-VN-0001" đã có trong danh sách Lô Gom hiện tại. | 1. Quét lại mã "ELG-VN-0001" lần 2. | Tracking code: "ELG-VN-0001". | App phát âm thanh Boop, hiển thị toast "Mã vận đơn đã có trong lô gom hiện tại!". Số đếm không tăng. | App hiển thị cảnh báo trùng mã, giữ nguyên số đếm. | Pass |
| **TC_UC12_21** | Seller từ chối ký bàn giao toàn bộ (Cancelled Session Audit). | Shipper đã quét 5 đơn, Seller từ chối ký biên bản. | 1. Shipper chọn "Hủy phiên gom - Seller từ chối ký". | Lý do: "SELLER_REFUSED_ALL". | Rollback trạng thái cả 5 đơn, lưu bản ghi Audit Log vết phiên bị hủy status: CANCELLED. | Rollback 5 đơn thành công, ghi Audit Log phiên hủy. | Pass |
| **TC_UC12_22** | Dynamic Eviction UX khi 1 đơn bị hủy/gán lại mid-session. | Lô 5 đơn, có 1 đơn ở DB bị Seller bấm CANCELLED giữa lúc đang quét. | 1. Shipper nhấn Chốt ePOH cho 5 đơn. | 4 đơn READY_TO_PICK, 1 đơn CANCELLED. | Tự động loại bỏ đơn CANCELLED ra khỏi lô, thông báo chi tiết mã đơn bị loại và chốt ePOH thành công cho 4 đơn còn lại. | Loại bỏ đơn bị hủy ra khỏi lô, chốt ePOH thành công 4 đơn. | Pass |
| **TC_UC12_23** | Idempotency khi Worker retry đồng bộ Offline lặp lại. | Phiên gom offline có clientOfflineId = "OFFLINE_BATCH_999". | 1. Sync Worker gửi trùng payload lần 2 với cùng clientOfflineId. | clientOfflineId: "OFFLINE_BATCH_999". | Sparse Unique Index chặn duplicate, trả về kết quả ePOH đã sinh ở lần 1 mà không nhân bản CSDL. | Trả về kết quả ePOH cũ, không ghi trùng CSDL. | Pass |
